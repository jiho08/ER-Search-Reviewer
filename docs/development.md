# 개발 기록

## 2026-09-16 · 공개 사이트 검증과 상세 기록 없는 플레이어 처리

[Cloudflare 공개 사이트](https://lumia-er-reviewer.57872222k.workers.dev/)에서 `ER_API_KEY` 적용과 규칙 기반 리뷰를 확인했다. 명시적 HTTP 검사 13개와 `scripts/smoke.mjs`를 통과했으며, 키가 등록되어 미설정 키 오류 검사는 조건에 따라 건너뛰었다. 물방개 시즌 12(API ID 41)의 7,186 RP와 공식 누적 171경기를 확인하고 33페이지에서 랭크 171경기를 확보했다. 같은 조회의 최근 최대 100경기 기본 리뷰에서 분석 도구 4개 실행을 확인했다. 유료 AI·Codex 모델은 호출하지 않았다.

이후 특정 플레이어의 검색 실패를 조사했다. `병관이아트수제자`의 닉네임 조회는 HTTP 200과 유효한 UID를 반환했지만 최근 경기 응답의 본문 `code`는 404였다. 키 인증 실패나 닉네임 부재가 아니라 상세 기록이 없는 상태를 전체 검색 실패로 처리하던 문제였다.

경기 목록 경로의 HTTP 404·본문 404만 빈 경기로 처리하고 시즌 목록·랭크·누적 성적 조회를 이어 가도록 수정했다. 다음 페이지의 404는 조회 완료로 처리하며, 닉네임 오류·인증 오류·429 호출 제한·기타 502 오류는 빈 경기로 숨기지 않는다. 누적 API의 `userStats: []`는 명시적 0경기·0승으로 표시하고 응답 실패나 누락은 미제공 상태를 유지한다. 화면은 이전 시즌 선택을 안내하고 상세 기록이 없을 때 리뷰 버튼을 비활성화한다.

수정된 로컬 서버에서 해당 플레이어의 현재 시즌 12와 과거 시즌 11 조회가 HTTP 200, 상세 0경기, 선택 가능한 시즌 41개로 응답했다. 공식 누적 API도 시즌 7~12에서 빈 배열을 반환했으므로 해당 사용자의 과거 기록이 있다고 추정하지 않았다. 별도로 물방개의 시즌 11(API ID 39) 누적 252경기·39승·7,665 RP를 확인해 과거 누적 성적은 최근 90일 상세 기록과 구분됨을 확인했다.

회귀 테스트 45개와 타입 검사·공개용 빌드·배포 dry-run을 통과했다. 변경 코드 5개 파일의 ESLint는 오류·경고 0개였고, 전체 lint는 실행 시간이 길어 중단했으므로 완료로 기록하지 않는다. 실제 브라우저에서 검색 후 시즌 선택지 41개가 활성화되고 시즌 11로 전환되며 상세 0경기에서 기본/Codex 리뷰가 비활성화됨을 확인했다. 이 수정의 확인은 로컬 기준이며 위 공개 사이트 통과 기록은 수정 전 배포본에 대한 것이다. 최신 공개 반영 결과는 README 검증에 구분해 기록한다. README와 배포·API 조사 안내에 실제 공개 상태, 상세/누적 기록 차이와 검증 범위를 함께 반영했다.

## 2026-09-16 · 공개 배포 코드 업로드 점검

공개 배포 준비 변경 28개 파일의 업로드 대상을 확인했다. 실제 환경 설정·인증·실행 캐시는 Git에서 제외되어 있으며 변경 파일의 일반적인 비밀 키 패턴 검사에서 일치 항목을 발견하지 않았다. README의 갱신 날짜와 두 환경 설정 예시 파일 안내를 현재 상태에 맞췄다. 이번 업로드 점검에서는 앱 코드를 추가 변경하지 않았고 아래의 테스트 결과를 유지하며 실제 ER·AI 호출과 Cloudflare 배포는 하지 않았다.

## 2026-09-16 · Cloudflare 공개 배포 준비

공개 사이트 전환의 코드 준비 단계로 별도 Worker 진입점·Wrangler/Vite 설정·공개 빌드와 dry-run·미리보기 명령을 추가했다. 로컬 실행·Codex 연결은 유지하고 공개용은 루트 `.env`와 개인 인증을 읽지 않는다. Cloudflare 계정 연결·실제 배포·도메인 연결은 진행하지 않았다.

공식 ER API 키 하나의 호출 간격과 전적 페이지 상태를 Durable Object의 SQLite에 모았다. 전적을 페이지별로 저장하고 페이지와 커서 갱신은 원자적으로 처리한다. 동일 검색의 진행 중 요청과 60초 캐시를 공유하고, 재시작 후 페이지 재개·중복 요청·화면에서 확인한 페이지 범위의 리뷰를 유지한다. 임시 저장은 마지막 조회부터 30분·최대 200개 세션이며 영구 시즌 아카이브는 아니다.

공개 검색·페이지·리뷰의 IP별/전체 제한, 리뷰 동시 2개 슬롯과 5분 만료, UTC 하루 AI 리뷰 시도 건수 제한을 저장소에 기록한다. 원본 IP는 저장하지 않고 서버 비밀값을 사용하는 날짜별 HMAC으로 식별한다. 공개 기본값은 규칙 기반 분석이며 유료 API는 공급자·Secret·양수 일일 한도를 모두 설정해야 활성화된다. 개인 Codex 설정은 공개 서버에서 거부한다.

기존 34개 테스트에 공개 서버 테스트 7개를 추가해 41개를 통과했다. 실제 SQLite 파일을 닫고 다시 열어 페이지·커서를 복원했고, 쓰기 실패 시 롤백, 새 제한기 인스턴스에서도 사용량 유지, 요청 큐 상한, UTC 한도 초기화, 4KB 초과 스트림과 비정상 요청 거부를 검사했다. 타입 검사·lint(오류/경고 없음)·공개 빌드·Wrangler dry-run, 공개용 로컬 HTML·예시 20경기·기본 리뷰 및 기존 로컬 서버 HTTP 검사를 통과했다. 실제 ER·AI 모델 호출은 하지 않았다.

첫 공개용 로컬 실행에서는 현재 날짜를 지정한 Worker를 설치된 workerd가 지원하지 않는 오류가 발생했다. 호환 날짜를 설치 버전의 최대 지원 날짜인 `2026-05-22`로 고정해 실행을 확인했다. 빌드에는 기존 프레임워크의 동적 import·큰 클라이언트 청크 경고가 남아 있으며 성능 최적화 완료로 기록하지 않는다. 배포·설정·운영 범위는 [Cloudflare 배포 안내](cloudflare-deployment.md)에 정리했다.

실제 로컬 Worker에서도 내부 식별자 헤더를 매번 바꾸며 검색했을 때 같은 IP의 7번째 요청부터 `429`와 `Retry-After`가 반환되는 것을 확인했다. 잘못된 리뷰 요청을 Durable Object가 일찍 거부하면 원본 요청 스트림을 응답 후에 읽으려는 workerd 오류가 발생했다. 경계에서 본문을 최대 4KB로 제한해 읽은 후 전달하도록 수정했다. 공개용 재빌드 중에는 기존 미리보기를 중지해야 파일 교체 도중 모듈이 사라지는 오류를 피할 수 있다.

추가 조사에서 로컬 프록시가 작은 오류 요청의 읽지 않은 본문 때문에 후속 POST에 재시작 안내를 반환하는 현상을 확인했다. 허용 본문은 4KB로 유지하되, 작은 거부 본문은 버퍼에 쌓지 않고 제한적으로 소비한 뒤 응답하도록 보완했다. 전송 소비는 최대 64KB·5초로 제한한다. 공개용 개발 레지스트리도 개인 개발 환경과 분리했다.

최종 공개용 로컬 실행에서 없는 API 6회는 모두 404, 4KB 초과 요청 4회는 모두 413을 반환했고 이어서 정상 예시 검색·리뷰와 HTTP 검증을 통과했다. 최종 41개 테스트·타입 검사·lint·빌드·배포 dry-run도 통과했다. 문서 6개의 로컬 링크 16개를 확인했으며 README에는 수행평가 관련 표현을 추가하지 않았다.

## 2026-09-15 · README 소개 정리

README를 일반 사용자를 위한 프로젝트 소개·사용법·개발·검증 안내 중심으로 정리했다. 문서 내용과 로컬 링크를 확인했으며 앱 코드 변경이나 테스트 재실행은 하지 않았다.

## 2026-09-15 · GitHub 코드 공유

사용자가 제공한 복제 폴더에서 `jiho08/ER-Search-Reviewer` 저장소를 확인했다. 원격에는 프로젝트명과 소개를 담은 README 및 기존 커밋 2개가 있었고, 현재 작업 폴더에는 최초 구현 커밋과 이후 기능·문서 수정이 있었다. 저장소의 소개를 README에 반영하고 기존 양쪽 이력을 보존해 통합했다.

업로드 후보 144개 파일을 확인했다. `.env`, 인증·실행 캐시, 의존성, 빌드 결과는 제외되며 `.env.example`의 키 입력란은 비어 있다. 일반적인 키 패턴·인증 파일 이름과 1MB 이상 파일은 발견하지 않았다. 변경 사항은 `59f97f6`에 저장하고 기존 GitHub 이력을 병합한 `483e45e`를 일반 push로 업로드했다. 원격 `main`이 `483e45e4ef40b7d5b872cfa2d55ec889af42b453`와 일치함을 확인했다. README와 과제 현황에 완료 상태·저장소 링크를 반영했다. 이 작업에서 앱 코드는 추가 수정하지 않았으며 기존 34개 자동 테스트·타입 검사 결과를 유지한다. 사이트 외부 배포는 진행하지 않았다.

## 2026-09-15 · 수행평가 점검과 문서 현행화

과제 안내문·결과보고서와 현재 코드를 대조했다. 전적 조회와 Codex 예시 리뷰는 각각 검증되어 있지만 본인 전적의 최종 시연·캡처, 직접 작성한 후기, 블로그/SNS 게시, GitHub 업로드는 제출 증빙이 남아 있다. 과제에는 공개 서비스 URL 제출이 명시되어 있지 않으며 “정상 배포·실행”의 구체적 인정 범위는 교사의 별도 안내를 확인한다.

점검 과정에서 34개 자동 테스트와 타입 검사를 다시 통과했다. 테스트는 모의 ER·AI 응답과 로컬 프로세스를 사용했으며 실제 ER·Codex 요청은 반복하지 않았다. lint·빌드·HTTP 검사는 2026-09-14의 기존 검증 기록으로 구분한다.

README의 ER 키 미작동·이미지 미연결·시즌 누적 성적 미수집 설명을 현재 상태에 맞게 수정했다. 시즌 전체와 날짜별 RP 선 연결, 경기 결과 색상, 실험체 지표, 필터 범위, 실제 도구 등록·호출 방식과 검증 날짜를 반영했다. 관련 안내의 오래된 현재 상태 설명을 함께 갱신하고, 앞으로 수정 작업마다 README를 갱신하는 지침을 `AGENTS.md`에 추가했다. 변경한 문서 6개의 로컬 링크 10개와 현재 상태 설명·문서 차이를 확인했다. 문서 수정 후 앱 테스트는 반복하지 않았으며 앱 동작도 변경하지 않았다.

## 2026-09-14 · 시즌별 전체 전적 조회

실제 API에서 응답 `next`와 `?next=`로 다음 경기 묶음을 조회하는 동작을 확인했다. 시즌 선택 시 해당 시즌의 제공 가능한 기록을 끝까지 수집하고 100경기 표시 제한을 제거했다. 진행 상황, 일시 중지·재개, 오류 후 기존 기록 유지, 중복 제거, 다른 시즌 제외를 처리했다. 그래프 기본 기간은 시즌 전체이며 과거 시즌은 종료일까지 표시한다.

공식 누적 시즌 경기 수와 확보한 상세 기록 수를 구분해 보여준다. 최근 90일·닉네임 변경 전 기록 제한은 남아 있어 조회 완료를 시즌 전체 보장으로 표시하지 않는다. 리뷰는 화면이 수신한 페이지 범위를 서버에서 재구성하고 그중 최근 최대 100경기를 분석한다. 일시 중지한 요청이 서버에서 뒤늦게 완료돼도 분석 표본이 늘어나지 않도록 보완했다.

물방개 시즌 12를 실제로 33페이지 조회해 랭크 170경기를 확보했으며 공식 누적 170경기와 일치했다. 기록은 한국 시간 8월 13일부터 9월 13일까지였다. 34개 자동 테스트, 타입 검사, lint, 빌드, 로컬 HTTP 스모크 검사를 통과했다. 실제 Codex 모델 호출과 외부 배포는 하지 않았다.

## 2026-09-14 · 날짜별 RP와 경기 결과 표시 수정

RP 그래프를 한국 시간의 경기 시작일 기준으로 묶고 최근 7일·30일·90일을 선택하도록 변경했다. 일별 RP는 그날 마지막 경기의 종료 점수, 일별 증감은 제공된 증감의 합계다. 조회되지 않은 날과 누락 점수는 빈 값으로 유지하며, 기간 선택은 이미 조회된 경기 범위에 적용된다.

1등의 기존 색상을 유지하고 2~3등은 파랑, 탈출 성공은 초록, 탈출 실패는 빨강으로 표시한다. 1등 외에는 공식 탈출 결과가 순위 색상보다 우선한다. 순위 흐름을 제거하고 실험체별 표를 승률·RP 증감·경기 수·평균 TK/K·평균 딜량·평균 시야 점수·평균 동물 딜량으로 구성했다.

29개 자동 테스트, 타입 검사, lint, 빌드, 로컬 HTTP 스모크 검사를 통과했다. 실제 물방개 전적 10경기에서도 동물 피해량과 탈출 상태를 확인했고, 날짜별 집계는 9월 12일 3경기·+73 RP, 9월 13일 7경기·+205 RP로 검증했다. 이번 변경에서는 실제 Codex 모델을 추가 호출하지 않았다.

## 2026-09-14 · 정상 닉네임 검색 실패 수정

실제 닉네임 조회는 HTTP 200으로 성공했지만 응답 식별자 이름이 `user.userId`여서 문서 예시의 `user.uid`만 확인하던 코드가 잘못된 404를 반환했다. 실제 응답 형태로 동일 오류를 재현하는 테스트를 먼저 추가한 뒤 두 이름을 호환 처리했다. 식별자 형식 오류는 닉네임이 없다는 안내와 구분해 502로 처리한다.

수정된 사이트의 공식 API 연동으로 `물방개` 실제 전적 10개, 시즌 12의 7,030 RP와 시즌 성적 조회 성공을 확인했다. 이제 이 키의 인증과 기본 전적 조회는 동작한다. 이전 403은 당시 관측 기록이며 현재 상태를 뜻하지 않는다. 응답 진단에서는 키나 식별자 값을 출력하지 않았고 `.env`를 변경하지 않았다.

## 2026-09-14 · 이미지·티어·상세 전적 확장

사용자가 지정한 DAK.GG 물방개 전적 화면의 정보 구성을 참고했다. 라임·흑연색 팔레트를 유지하고 현재 시즌 티어·RP·순위, RP 추이와 경기별 증감 그래프, 12개 지표, 실험체별 성적 표, 캐릭터·장비·특성·전술 스킬 이미지를 추가했다. 이미지는 공개 카탈로그의 검증된 URL을 사용하며 21개 예시 이미지의 HTTP 응답과 이미지 형식을 확인했다.

현재 시즌 랭크는 공식 시즌·랭킹·누적 성적 API를 조인한다. 시즌 정보가 없으면 현재 시즌을 추측하지 않고, 순위 요청이 실패해도 최근 경기를 표시한다. 점수 그래프는 같은 시즌의 실제 제공값만 연결하며 절대 RP가 없으면 증감 그래프를 사용한다. 메테오라이트 이하의 단계는 공식 기준으로 계산하고, 추가 승급 조건을 확인할 수 없는 고 RP 구간은 미스릴 이상으로 표시한다.

25개 자동 테스트, 타입 검사, lint, 사이트 빌드, 로컬 HTTP 스모크 검사를 통과했다. 0/미제공 구분, 시즌 분리, RP 누락과 티어 경계값, 시즌 API 일부 실패 복구, 기존 Codex 연결 계약 유지를 검증했다. 이번 확장에서는 ER 개인키 확인과 실제 Codex 모델 호출을 반복하지 않았다. 실제 플레이어 신규 항목 검증은 ER 키 활성화 이후 남아 있다. [조사 근거와 제약](research/player-dashboard.md)에 데이터 범위·출처·이미지 갱신 방법을 정리했다.

## 2026-09-14 · Codex 계정 리뷰 연결

사용자의 선택에 따라 별도 OpenAI API 키 대신 현재 PC의 Codex ChatGPT 로그인을 사용하는 로컬 연결을 추가했다. Node.js 브리지가 표준 입출력으로 Codex App Server와 통신하며 기존 분석 도구를 재사용한다. 사이트 서버에서 전적을 다시 확보하고 브리지는 고정된 전적 스키마만 받는다.

실제 계정의 예시 3경기 분석과 사이트 HTTP API를 통한 `engine=codex` 응답을 확인했다. 전적 조회·생존 분석 도구 호출, 표본 한계와 가상 데이터 표시를 확인했다. 18개 자동 테스트와 타입 검사·lint·사이트 빌드가 통과했다. Codex 로그인 정보는 읽거나 복사하지 않았으며 별도 브리지 인증 토큰만 Git에서 제외된 `.env`에 생성했다.

연결 과정에서 Worker의 `redirect:error` 미지원과 Codex 실행 환경 전체 비활성화 시 분석 도구도 막히는 문제를 확인했다. 리다이렉트를 자동으로 따르지 않는 `manual` 검사로 변경하고, Codex의 명령·파일 편집·외부 기능을 개별 비활성화했다. 실제 사용 한도 오류의 문자열 형태와 필수 분석 도구 누락, 초기화를 포함한 전체 제한 시간도 검증에 반영했다.

`npm start`가 빌드 디렉터리 대신 루트 `.env`를 읽도록 절대 경로를 전달하게 변경했다. 빌드된 Worker에서도 예시 20경기의 종합 리뷰와 네 분석 도구 실행을 실제 검증했다. ER 개인키의 초당 1회 제한에 맞춰 요청 간격도 1.1초로 보완했다. 입력된 ER 키로 `/v2/data/Character`를 요청했으나 `403 ForbiddenException`을 받아 본인 전적 검증은 남아 있다. 공개 배포는 하지 않았다. 실행과 제한은 [Codex 연결 안내](codex-setup.md)에 정리했다.

## 2026-09-14 · 첫 로컬 구현

### 목표와 범위

과제 문서의 자유 주제로 이터널 리턴 전적 검색·개인 리뷰를 선택했다. 첫 범위는 닉네임 조회, 최근 경기 표시, 필터·지표, 리뷰 생성까지 이어지는 한 흐름이다. 참고 서비스 이름은 아디나의 분석실이며 디자인이나 기능 전체를 복제하지 않았다.

### 공식 API 조사에서 반영한 변경

최신 문서의 UID 기반 경로를 사용했다. 이전 사용자 번호를 사용하지 않는다. 경기의 `characterNum`, `playerAssistant`, `playTime` 필드를 정확히 연결했고, RP 누락을 0으로 대체하지 않는다. 시간대가 없는 시각은 추측하지 않는다. 자세한 출처와 미확인 사항은 [API 조사](research/eternal-return-api.md)에 기록했다.

### 구현 결정

- 브라우저에는 정규화된 최소 전적만 전달하고 API 키는 서버 환경 변수에 둔다.
- 규칙 분석도 등록된 도구를 주제에 따라 선택·실행하도록 만든다.
- 실제 AI에서는 모델의 함수 호출을 실행하고 도구 출력과 call_id를 연결해 다음 요청에 전달한다.
- AI 오류가 발생하면 기본 분석으로 대체하되 화면에 대체 사실을 명시한다.
- 통계와 리뷰는 같은 모드·실험체 범위를 사용한다. 오래된 비동기 결과가 새 플레이어·필터의 리뷰를 덮어쓰지 않게 요청 번호를 확인한다.
- 예시 모드는 가상 기록으로 명시하며 유료 모델을 호출하지 않는다.

### 발견한 문제와 해결

1. **Windows 폴더 이름의 `&` 문자 때문에 npm postinstall 실패**: cmd.exe의 패키지 실행 shim에서 경로가 분리됐다. Windows의 설치 자식 프로세스만 PowerShell을 사용하도록 변경해 설치를 완료했다.
2. **새로고침이 캐시를 그대로 사용**: 화면의 새로고침 버튼이 `refresh=1`을 보내도록 연결했다. 일반 검색에는 60초 캐시를 유지한다.
3. **정렬 전 100개 제한으로 최신 기록 누락 가능**: 응답 전체 정규화·중복 제거·정렬 후 제한하도록 수정했다. 오래된 순서로 101개를 받은 테스트로 검증했다.
4. **순위 미제공 값이 있는 비교 구간**: 구간 길이 대신 유효 순위가 각각 최소 3개인지 확인해 잘못된 개선 수치를 막았다.
5. **브라우저 API 입력 조작**: 리뷰 요청 스키마에 전적 수치 필드를 허용하지 않고, 서버가 전적을 다시 가져오게 했다.

### 검증 범위

13개 단위 테스트, TypeScript 검사, 작성 코드 lint, 클라이언트·서버 프로덕션 빌드를 실행했다. 자동 테스트는 공식 API·OpenAI 모의 응답을 사용한다. 브라우저에서 예시 전적, 모드 필터, 경기 상세, 리뷰 생성, 실행 도구 표시를 확인했다. 1440px·390px·320px 화면에서 가로 넘침이 없는지 확인했고, WebMCP 검색의 정상 입력·빈 입력 거부를 확인했다. 개발 서버와 빌드된 Worker 서버 양쪽에서 HTTP 검증 스크립트가 통과했다. 실제 계정 키를 이용한 전적·AI 응답 검증과 외부 배포는 아직 남아 있다.

### 다음 작업

1. 공식 API 키로 본인 닉네임을 조회하고 실제 응답 필드·현지화·시간대를 확인한다.
2. AI 키 연결 후 실제 수치와 리뷰 문장 일치 여부를 확인한다.
3. 사용자가 직접 경기 전후에 사용하며 후기와 개선점을 기록한다.
4. 외부 공개 조건과 비용 통제를 갖추고 배포한 다음 GitHub·블로그 링크를 정리한다.
